class STRING

feature
  make_empty
    do
    end

  substring (start : INTEGER ; finish : INTEGER) : STRING
    do
    end

  last_index_of (c : CHARACTER; i : INTEGER) : INTEGER
    do
    end

  count : INTEGER

  starts_with (s : STRING) : BOOLEAN
    do
    end

  split (c : CHARACTER) : LIST [STRING]
    do
    end

  append (s : STRING) : STRING
    do
    end

  twin : STRING
    do
    end

  is_equal (s : STRING) : BOOLEAN
    do

    end
end
